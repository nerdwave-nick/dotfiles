#!/bin/bash

REPO_DIR="$(realpath "$(dirname "$(realpath "${BASH_SOURCE:-$0}")")/../../")"
R_DOTCONFIG_DIR="${REPO_DIR}/dotconfig"
R_SCRIPTS_DIR="${REPO_DIR}/scripts"
R_WALLPAPERS_DIR="${REPO_DIR}/wallpapers"
R_SETTINGS_DIR="${REPO_DIR}/.settings"
R_INSTALL_DIR="${REPO_DIR}/.install"
H_DOTCONFIG_DIR="${HOME}/.config"
